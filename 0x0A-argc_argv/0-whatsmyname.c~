#include <stdio.h>
/**
 * main - a program that prints its own name, followed by a new line.
 *@argc: argument cuantitie.
 *@argv: argument
 *Return: 0.
 */

int main(int argc, char *argv[])
{
int i = argc
printf("%s\n", argv[i] - 1);
return (0);
}

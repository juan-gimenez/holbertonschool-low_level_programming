#include <stdio.h>
#include <stdlib.h>
/**
 * main - Prints the alphabet.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
 num[120] = "0123456789";
int i;
for (i = 0; i < 11; i++)
if (i < 10)
putchar(num[i]);
else
putchar('\n');
return (0);
}

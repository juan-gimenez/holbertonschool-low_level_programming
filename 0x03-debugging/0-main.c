#include "main.h"

/**
 * main - tests function that prints if a number is positive or negative
 * Return: 0
 * @posorneg: value to consider
 */

int main(void)
{
int posorneg;
posorneg = 0;
positive_or_negative(i);
return (0);
}

#include "main.h"

/**
 * print_rev - print string in reverse
 * Return: no return
 * @s: pointer
 */
void print_rev(char *s)
{
int i = 99;
int str[100];
while (str >= 0)
{_putchar(str[i]);
i--; }
_putchar(10);
}

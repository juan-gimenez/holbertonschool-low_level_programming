#include "main.h"
#include <stdio.h>
void print_to_98(int n)
{
  int n;
  while (n > 98)
    {
      printf("%d, ", x);
      n--;
    }
  while (n < 98)
    {
      printf("%d, ", n);
      n++;
    }
  if (n == 98)
    printf("%d", n);
}

#include "main.h"
/**
 * _islower - if  c is lowercase return 1 if not 0
 * Return: void
 * @c: integer
 */
int _islower(int c)
{
  if (c >= 97 && c <= 122)
    {
      _putchar(49);
    }
  else if
    {
      _putchar(48);
    }
  return (0);
}

#include "main.h"
/**
 * _isdigit- Entry point
 * @c: parameter
 *
 * _isdigit:if c is a digit
 * Return: 1 if digit , 0 other
 */
int _isdigit(int c)
{
if(c >= 48 && c <= 57)
{    
return (1);
}
else
{
return (0);
}
}

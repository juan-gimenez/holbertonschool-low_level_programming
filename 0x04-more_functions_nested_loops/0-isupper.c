#include "main.h"
/**
 * _isupper - Entry point
 * @c: parameter
 *
 * _isupper:if a letter is uppercase
 * Return: 1 if upercase , 0 other
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
